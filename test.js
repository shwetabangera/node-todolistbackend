/* const dotenv = require("dotenv");

console.log(process.env);
dotenv.config({ path: "./config.env" });
console.log(process.env.PORT);
 */
const uniqid = require("uniqid");
console.log("re" + uniqid());
